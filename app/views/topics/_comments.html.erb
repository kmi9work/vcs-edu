<% @topic.comments.each do |comment| %>
	<%= h comment.content%> | 
	<%= link_to 'Править', edit_comment_path(comment) %> | 
	<%= link_to 'Удалить', comment, :confirm => 'Вы уверены?', :method => :delete %><br>
	<hr>
<% end %>
